# ServiceNow Development Notes

Load this file when creating or reviewing ServiceNow scripts, widgets, or API-heavy changes.

## Table API

- Use `sysparm_display_value=false` for follow-up automation, `true` for concise readable output, and `all` when the response must include display values and sys_ids.
- For complex `sysparm_query` values, prefer encoded queries copied from the platform filter breadcrumbs. ServiceNow's developer course notes that encoded query syntax is not documented and is best generated by the platform.
- Session debug can be requested with `X-WantSessionDebugMessages: true`, but it only returns useful details when session debugging is enabled in the instance.

## Core Organizational Data

- `cmn_department` already supports department hierarchy through the standard `parent` reference to `cmn_department` and department head/manager assignment through the standard `dept_head` reference to `sys_user`. Use a custom field such as `u_level` only for organization-specific hierarchy classification, and create that dictionary change in a Global update set; seeded department rows themselves are live data and are not normally represented as update-set application files. If the business wants a numeric level field, keep both `sys_choice.value` and `sys_choice.label` numeric (`1`, `2`, `3`) so forms and lists do not show descriptive text in the field.

Source: ServiceNow Developer REST/Table API course, inbound REST integrations.
https://developer.servicenow.com/print_page.do?category=course-module&identifier=app_store_learnv2_rest_yokohama_inbound_rest_integrations%2Capp_store_learnv2_rest_yokohama_cors_rules&module=course&release=yokohama

## Outbound REST Integrations

- Load `references/integrations.md` before designing or changing outbound REST integrations.
- Start with a read-only recordless `sn_ws.RESTMessageV2()` probe in Xplore to prove network, TLS, auth, and payload shape.
- For durable work, create REST Message and HTTP Method records, then call them from a Script Include wrapper. Keep secrets in auth profiles or connection/credential aliases, never in script.
- Verify each integration in three layers: transport, mapping, and persistence/idempotency.
- Query `sys_outbound_http_log` after tests to confirm `url`, `method`, `response_status`, and `response_time`.
- For SAP SuccessFactors, plan for OData and OAuth 2.0 unless the project explicitly provides another supported contract.

## Story Delivery Workflow

Load `story-delivery.md` for story-number or story-style work. It owns requirements-source routing, acceptance-matrix execution, update-set reuse, validation, state/comment authorization, and the ready-to-paste work note. Do not create an `rm_story` for an ordinary ad hoc task.

## Server-Side Verification Snippets

- Prefer `scripts/Invoke-ServiceNowXploreScript.ps1` when a verification needs ServiceNow server-side execution. It posts to Xplore's `/snd_xplore.do?action=run` processor and receives JSON, which is easier to parse and avoids the `sys.scripts.do` GET URL length problem.
- Use `scripts/Invoke-ServiceNowBackgroundScript.ps1` only when Xplore is not installed, the Xplore processor is blocked, or you need to compare behavior with the platform Scripts - Background UI.
- Keep snippets self-contained with an IIFE. Avoid relying on ambient variables such as `current`.
- Constrain all queries with explicit conditions and `setLimit()` unless using an aggregate count.
- Normalize boolean fields explicitly when a script selects or excludes records. Table API responses commonly expose `true`/`false`, while `GlideRecord.getValue()` can expose `1`/`0`; use the field's boolean accessor when available or accept both representations, then post-verify every exclusion category before declaring a bulk identity/user update complete.
- For verification output, print one small JSON object between `SN_RESULT_START` and `SN_RESULT_END`.
- Pass `-Scope <sys_scope.scope>` or `-ScopeSysId <sys_scope.sys_id>` when the Xplore script must execute in a specific application scope. Resolve the scope through the Table API first.
- If scoped execution returns an authorization/scope error, retry the check globally only when the logic does not require scoped execution. Some scoped app contexts can block script execution even for admin.

Command examples: `examples.md`.

Sources:
- Xplore homepage: https://xploretoolkit.com/
- Xplore source, processor endpoint and `run` action: https://github.com/jneale/Xplore/blob/master/src/Server%20Development/Processors/snd_xplore.script.js
- ServiceNow Developer Blog, "Using Scripts - Background": https://developer.servicenow.com/blog.do?p=%2Fpost%2Ftraining-scriptsbg%2F
- ServiceNow Community references `sys.scripts.do` as the Scripts - Background endpoint: https://www.servicenow.com/community/developer-forum/what-is-script-background-sys-scripts/m-p/1966691

## Update Sets

- Always identify the original artifact's `sys_scope` before creating an update set. Create the update set in that same application scope when cloning or modifying scoped artifacts.
- To make an update set current for a scoped app through API automation, set two `sys_user_preference` records for the developer user:
  - `apps.current_app = <sys_scope.sys_id>`
  - `updateSetForScope<sys_scope.sys_id> = <sys_update_set.sys_id>`
- Normal Table API creation can still inherit the developer's current app scope even when an `application` value is supplied. Always verify `sys_update_set.application` after creating a scoped update set; if it is wrong, set the current app preference before recreating it or use a constrained admin/Xplore correction for the update-set record.
- Table API inserts/patches can update records without reliably creating `sys_update_xml` entries in the intended update set. Prefer `scripts/Confirm-ServiceNowUpdateCapture.ps1`; manually verify with:

```text
update_set=<sys_update_set.sys_id>
```

- Use `scripts/Save-ServiceNowCustomerUpdate.ps1` only after diagnosing missing natural capture for a legitimate application file such as `sysauto_script`. Verify `sys_update_xml.update_set` and application. If capture lands in Default or another set, correct the execution scope/update-set context and recapture; do not move customer updates or remove unrelated rows to disguise the mismatch.
- Table API `DELETE` removes a record but does not reliably create the deployable `DELETE` customer update. For an explicitly approved deletion of a proven customer-created artifact, use `scripts/Remove-ServiceNowArtifactWithDeleteCapture.ps1`: it validates the exact record and application, snapshots it with `GlideUpdateManager2`, converts that customer update and its payload to `DELETE`, performs the Table API deletion, and verifies both absence and capture. Never use it for an OOTB record, and preview the resulting update set in the target before commit.
- `sys_update_xml.name` for a Service Portal widget is usually `sp_widget_<sp_widget.sys_id>`. Query this when verifying that a cloned widget was captured.
- A useful final update-set check is: exactly one `sys_update_xml` row for the target record exists in the intended update set, its payload contains the latest changed script or marker string, and every row in that update set has `sys_update_xml.application` equal to `sys_update_set.application`.

### Update Set Batches

- Update set batching is represented directly on `sys_update_set` in this PDI:
  - parent batch record: `parent` empty, `base_update_set` points to itself, always create it in Global scope (`application=global`)
  - child update sets: both `parent` and `base_update_set` point to the batch update set sys_id
- Before batching story update sets, query all similarly named update sets and summarize each candidate with `Get-ServiceNowUpdateSetSummary.ps1`. It is common to find earlier probe, empty, or mixed-scope update sets with the same story name.
- If the user asks to batch "these update sets" or refers to a matching story/name without narrowing the scope, include every matching update set. Do not silently omit empty, probe, stale, or mixed-scope-looking sets; ask first or include them and report the risk.
- Only exclude empty update sets, throwaway probe sets, stale duplicates, or mixed-scope sets when the user explicitly agrees to exclude them.
- For multi-scope story delivery, batch the per-scope update sets rather than merging scopes into one update set. Example pattern:

```powershell
$batch = @{
  name = 'STRY0013657 - Endring av personaltilhorighet Batch'
  application = 'global'
  state = 'in progress'
  description = 'Batch for STRY0013657 deployable update sets.'
} | ConvertTo-Json

& "$HOME/.codex/skills/servicenow-pdi/scripts/Invoke-ServiceNowTable.ps1" `
  -Method POST `
  -Table sys_update_set `
  -BodyJson $batch `
  -Fields 'sys_id,name,state,application,parent,base_update_set,description' `
  -DisplayValue all `
  -ExcludeReferenceLink `
  -Profile pdi
```

- After creating the batch, patch the batch's own `base_update_set` to its sys_id. Then patch each included child update set with both `parent=<batch_sys_id>` and `base_update_set=<batch_sys_id>`.
- Re-read the parent after that patch. On Vår Energi Sandbox, the Table API can return success while silently leaving a parent's self-referencing `base_update_set` empty; when that occurs, use one constrained Global GlideRecord update for the exact batch sys_id, then verify the parent and all children again.
- Verify with a readback query:

```text
sys_id=<batch_sys_id>^ORparent=<batch_sys_id>
```

- If similar named sets remain unbatched, report why they were skipped.

### Cross-Scope Privileges And Restricted Caller Access

- A `ScopeAccessNotGrantedException: write access to <table> not granted` can require a concrete `sys_scope_privilege` even when `sys_restricted_caller_access` already has an allowed source-scope-to-target-scope row.
- Inspect both tables before writing:
  - `sys_restricted_caller_access`: useful for caller restriction records, with integer `status` values such as Allowed = `2`
  - `sys_scope_privilege`: concrete cross-scope privilege rows, with string `status` values such as `allowed`
- For table writes, the narrow `sys_scope_privilege` shape is:

```json
{
  "target_name": "sn_hr_core_case",
  "target_type": "sys_db_object",
  "target_scope": "<target scope sys_id>",
  "operation": "write",
  "status": "allowed"
}
```

- Set the current application/update set context to the source scope before creating the privilege so `source_scope` defaults correctly and the privilege captures in the intended scoped update set.
- Verify the created privilege by reading `sys_scope_privilege`, then verify update capture as `type=Cross scope privilege` in `sys_update_xml`.

## Service Portal Theming

- Prefer changing portal theme records through Table API, then verifying compiled assets with an authenticated GET.
- For targeted dark-theme work, clone the current `sp_theme`, copy key fields (`css_variables`, `header`, `footer`, fixed header/footer flags), and point only the target `sp_portal.theme` to the clone.
- When copying reference fields from `-DisplayValue all` responses, use `.value`, not the whole object. Passing the whole object can create malformed reference strings.
- To add SCSS variables and selectors, patch `sp_theme.css_variables`; flush cache afterward.
- Use `sp_page.css` for page-specific overrides such as homepage cards.
- Verify the page uses the intended theme by fetching `/sp_config?...` and checking for `theme_id=<sp_theme.sys_id>` in the `sp-bootstrap-basic.scss` link.
- Verify compiled CSS by fetching that `styles/scss/sp-bootstrap-basic.scss?...` URL and searching for expected selectors/colors.
- Flush portal/theme CSS changes with a background script:

```javascript
(function () {
  gs.cacheFlush();
  gs.print('SN_RESULT_START' + JSON.stringify({flushed:true}) + 'SN_RESULT_END');
})();
```

- Service Portal page API is useful for checking returned page CSS:

```text
/api/now/sp/page?portal_id=<sp_portal.sys_id>&page_id=<sp_page.id>
```

- Legacy `sp_config` editor pages use old frontend libraries. Useful selectors include `.empty-state-wrapper .empty-state`, `.select2-container`, `.select2-drop`, `.select2-chosen`, `.CodeMirror`, `.ace_editor`, and generated widget wrappers such as `.v6e7bf89247301200ba13a5554ee490e3 .minibar`.

Source: ServiceNow Docs, "Defining portal styles": https://www.servicenow.com/docs/r/platform-user-interface/service-portal/portal-css.html

## Business Rules

- Keep advanced Business Rule code inside the generated function wrapper.
- Avoid shared global variable names such as `gr`; use specific names like `grIncident`.
- Do not call `current.update()` from a Business Rule unless there is a very deliberate reason. Before rules save current changes automatically; `current.update()` can recursively trigger rules.
- Keep Business Rules small and specific. Prefer Script Includes for reusable logic.
- Always use a condition when practical, and query narrowly inside the rule.
- For self-approval behavior on `sysapproval_approver`, use a narrowly scoped before rule that changes `current.state` instead of updating the approval or parent record recursively. Resolve the approving record through `document_id`; `source_table` can contain a base table while the document is an extension record, so do not assume it equals `getRecordClassName()`. Guard by the owning process or service, and test a matching approval plus a non-matching approval while checking and cleaning generated events or email rows.

Sources:
- ServiceNow Business Rules Technical Best Practices: https://developer.servicenow.com/dev.do?_escaped_fragment_=%2Fguides%2Fxanadu%2Fnow-platform%2Ftpb-guide%2Fbusiness_rules_technical_best_practices
- ServiceNow professional developer intro: https://developer.servicenow.com/print_page.do?category=now-platform&identifier=pro-dev-intro&module=guide&release=yokohama

## Script Includes

- Use Script Includes for reusable server-side logic rather than global Business Rules.
- For on-demand/classless Script Includes, the Script Include name must match the function name exactly.
- On-demand Script Includes are server-side only.

Source: ServiceNow Developer On Demand Script Include course.
https://developer.servicenow.com/dev.do?_escaped_fragment_=%2Flearn%2Fcourses%2Fxanadu%2Fapp_store_learnv2_scripting_xanadu_scripting_in_servicenow%2Fapp_store_learnv2_scripting_xanadu_server_side_scripting%2Fapp_store_learnv2_scripting_xanadu_on_demand_script_include

## Service Portal Widgets

- Client Script is the AngularJS controller and passes data/user input between template and server interactions.
- Server Script sets initial widget state, queries server-side data, and sends data to the Client Script through `data`.
- Link Function is optional and should be reserved for DOM work that the template/controller cannot handle cleanly.
- Use `option_schema` for reusable widgets; options are available through `options` server-side and through widget client/template APIs.
- Baseline widgets are often read-only; clone before editing baseline behavior.
- When overriding attachment links in a cloned widget, scope DOM click interceptors to a wrapper class owned by the clone. A document-level capture listener that rewrites every `sys_attachment.do` link can accidentally affect unrelated links elsewhere on the page.
- For attachment previews, `sys_attachment.do?sys_id=<attachment>` returns the file stream and may include `Content-Disposition: attachment`. Adding `view=true` as `sys_attachment.do?view=true&sys_id=<attachment>` returns the platform preview HTML for previewable files such as PDFs. This is a practical endpoint-level test for preview-vs-download behavior.
- The `/api/now/sp/widget/...` endpoint may return 405 for GET and an empty `{ result: {} }` for POST in this PDI; do not rely on it as the primary widget-render verification path. Prefer Table API record checks plus behavior-specific endpoint tests.

Source: ServiceNow Developer Service Portal course.
https://developer.servicenow.com/print_page.do?category=courses&identifier=app_store_learnv2_serviceportal_zurich_service_portal&module=course&release=zurich
