interface:
  display_name: "ServiceNow PDI"
  short_description: "Safe, OOTB-first ServiceNow engineering"
  default_prompt: "Use $servicenow-pdi to inspect the target instance, choose the safest OOTB-first solution, implement only the requested change, and prove behavior, delivery, and rollback."
